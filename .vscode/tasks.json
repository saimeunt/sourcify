{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "server:database-test:up",
      "type": "shell",
      "command": "cd services/server && export DOCKER_HOST_POSTGRES_TEST_PORT=${DOCKER_HOST_POSTGRES_TEST_PORT:-5431} && npm run postgres-test:start && sleep 2 && SOURCIFY_POSTGRES_HOST=localhost npm run postgres-test:migrate",
      "problemMatcher": []
    },
    {
      "label": "server:database-test:down",
      "type": "shell",
      "command": "cd services/server && npm run postgres-test:stop",
      "problemMatcher": []
    },
    {
      "label": "4byte:database-test:up",
      "type": "shell",
      "command": "cd services/4byte && npm run postgres-test:start && sleep 3 && FOURBYTES_POSTGRES_HOST=localhost FOURBYTES_POSTGRES_PORT=5433 npm run postgres-test:migrate",
      "problemMatcher": []
    },
    {
      "label": "4byte:database-test:down",
      "type": "shell",
      "command": "cd services/4byte && npm run postgres-test:stop",
      "problemMatcher": []
    }
  ]
}
